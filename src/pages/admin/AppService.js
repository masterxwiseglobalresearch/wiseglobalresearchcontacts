import React from 'react';

const AppService = () => {
  return (
    <div style={{height: '100%', width: '100%', minHeight: '600px'}}>
      <iframe
        src="https://wiseglobalresearch.in/"
        title="App Service"
        style={{ width: '100%', height: '80vh', border: 'none', minHeight: '600px' }}
        allowFullScreen
      />
    </div>
  );
};

export default AppService;
